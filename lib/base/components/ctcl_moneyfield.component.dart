import "package:flutter/material.dart";

class CtclMoneyField extends StatelessWidget {
  const CtclMoneyField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
